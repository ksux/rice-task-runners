{
    "default": ["build", "browserSync", "watch"],
    "build": ["clean:all", "copy", "styles", "scripts"],
    "styles": ["less", "autoprefixer", "concat", "csso", "clean:styles"],
    "scripts": ["browserify"]
}